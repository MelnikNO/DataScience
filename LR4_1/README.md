# Отчет по лабораторной работе №4_1

## Временные ряды. Основы. Модель AR

### Прогнозирование товарооборота книжного интернет-магазина

---

## 1. Введение

**Цель работы:** Построить модель прогнозирования товарооборота книжного интернет-магазина на основе временного ряда.

**Датасет:** `tovar_moving.zip`
- Столбцы: `date` (дата заказа), `qty` (количество книг)

**Задачи:**
1. Отложить последнее значение ряда для тестирования
2. Анализ тренда и сезонности
3. Прогноз с помощью экспоненциального сглаживания
4. Проверка на стационарность
5. Определение порядка модели AR
6. Построение модели AR и прогнозирование
7. Сравнение результатов

---

## 2. Подготовка и загрузка данных

### 2.1 Импорт необходимых библиотек

```python
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
from statsmodels.tsa.stattools import adfuller, kpss
from statsmodels.graphics.tsaplots import plot_acf, plot_pacf
from statsmodels.tsa.ar_model import AutoReg
from statsmodels.tsa.holtwinters import ExponentialSmoothing
from sklearn.metrics import mean_squared_error, mean_absolute_error
import warnings
warnings.filterwarnings('ignore')

# Настройка стилей
plt.style.use('default')
sns.set_style("whitegrid")
%matplotlib inline
```

### 2.2 Загрузка данных

```python
# Загрузка данных
df = pd.read_csv('tovar_moving.csv')

# Преобразование даты
df['date'] = pd.to_datetime(df['date'])
df.set_index('date', inplace=True)
```

**Информация о данных:**
- Количество записей: 1439
- Период: 25.12.2009 - 02.12.2013
- Пропуски отсутствуют
- Среднее количество заказов: ~281,488 книг

### 2.3 Откладывание последнего значения для тестирования

```python
# Сохранение последнего значения для теста
last_date = df.index[-1]
last_value = df.iloc[-1, 0]

# Удаление последнего значения из тренировочного набора
df_train = df.iloc[:-1]
df_test = df.iloc[-1:]
```

**Результат:**
- Последняя дата: 2013-12-02
- Последнее значение (тестовое): 423,846 книг
- Размер обучающей выборки: 1438
- Размер тестовой выборки: 1

---

## 3. Визуализация и анализ временного ряда

### 3.1 Визуализация ряда

```python
# Визуализация временного ряда
fig, axes = plt.subplots(2, 1, figsize=(14, 10))

# Исходный ряд
axes[0].plot(df.index, df['qty'], color='blue', linewidth=1.5)
axes[0].axvline(x=last_date, color='red', linestyle='--', label='Тестовая точка')
axes[0].set_title('Временной ряд товарооборота книжного магазина', fontsize=14)
axes[0].set_xlabel('Дата')
axes[0].set_ylabel('Количество книг')
axes[0].legend()
axes[0].grid(True, alpha=0.3)

# Распределение значений
axes[1].hist(df['qty'], bins=30, color='skyblue', edgecolor='black', alpha=0.7)
axes[1].axvline(df['qty'].mean(), color='red', linestyle='--', label=f'Среднее: {df["qty"].mean():.2f}')
axes[1].axvline(df['qty'].median(), color='green', linestyle='--', label=f'Медиана: {df["qty"].median():.2f}')
axes[1].set_title('Распределение количества заказов', fontsize=14)
axes[1].set_xlabel('Количество книг')
axes[1].set_ylabel('Частота')
axes[1].legend()
axes[1].grid(True, alpha=0.3)
```

### 3.2 Анализ тренда

```python
# Расчет скользящего среднего для выявления тренда
window_sizes = [7, 14, 30]

for idx, window in enumerate(window_sizes):
    df['MA_' + str(window)] = df['qty'].rolling(window=window).mean()
```

**Выводы по тренду:**
- Ряд имеет положительный тренд
- Среднее значение (281,488) > медианы (280,225)
- Скользящие средние показывают общий восходящий тренд с сезонными колебаниями

### 3.3 Анализ сезонности

```python
# Анализ сезонности по дням недели
df['day_of_week'] = df.index.dayofweek
df['day_name'] = df.index.day_name()

# Среднее количество заказов по дням недели
seasonal_pattern = df.groupby('day_of_week')['qty'].mean()
```

**Среднее количество заказов по дням недели:**
| День недели | Среднее значение |
|-------------|------------------|
| Понедельник | 298,544.39 |
| Вторник     | 292,941.45 |
| Среда       | 290,573.77 |
| Четверг     | 278,009.55 |
| Пятница     | 280,466.93 |
| Суббота     | 257,375.37 |
| Воскресенье | 272,586.82 |

**Выводы по сезонности:**
- Сезонность не выражена (коэффициент вариации = 0.050)
- Пик активности: Понедельник (в среднем 298,544.39 заказов)
- Минимальная активность: Суббота (в среднем 257,375.37 заказов)

---

## 4. Прогноз с помощью экспоненциального сглаживания

### 4.1 Построение модели

```python
# Модель экспоненциального сглаживания с α = 0.7
alpha = 0.7
model_es = ExponentialSmoothing(df_train['qty'], 
                                trend=None, 
                                seasonal=None, 
                                initialization_method="heuristic")
model_es_fitted = model_es.fit(smoothing_level=alpha)

# Прогноз на один шаг
forecast_es = model_es_fitted.forecast(1)
```

### 4.2 Результаты

| Показатель | Значение |
|------------|----------|
| Прогноз | 225,015.52 |
| Фактическое значение | 423,846.00 |
| Абсолютная ошибка | 198,830.48 |
| Относительная ошибка | 46.91% |

---

## 5. Проверка стационарности временного ряда

### 5.1 Тест Дики-Фуллера (ADF)

```python
def adf_test(series, title=''):
    print(f'Результаты теста ADF для {title}:')
    result = adfuller(series, autolag='AIC')
    print(f'ADF статистика: {result[0]:.4f}')
    print(f'p-value: {result[1]:.4f}')
    print('Критические значения:')
    for key, value in result[4].items():
        print(f'\t{key}: {value:.4f}')
    
    if result[1] <= 0.05:
        print("=> Ряд стационарный (отвергаем H0)")
        return True
    else:
        print("=> Ряд НЕ стационарный (не отвергаем H0)")
        return False
```

**Результаты ADF теста:**
- ADF статистика: -2.2553
- p-value: 0.1868
- Критические значения: 1%: -3.4350, 5%: -2.8636, 10%: -2.5679
- **Вывод: Ряд НЕ стационарный**

### 5.2 Тест KPSS

```python
def kpss_test(series, title=''):
    print(f'\nРезультаты теста KPSS для {title}:')
    result = kpss(series, regression='c')
    print(f'KPSS статистика: {result[0]:.4f}')
    print(f'p-value: {result[1]:.4f}')
    print('Критические значения:')
    for key, value in result[3].items():
        print(f'\t{key}: {value:.4f}')
    
    if result[1] <= 0.05:
        print("=> Ряд НЕ стационарный (отвергаем H0)")
        return False
    else:
        print("=> Ряд стационарный (не отвергаем H0)")
        return True
```

**Результаты KPSS теста:**
- KPSS статистика: 5.0734
- p-value: 0.0100
- Критические значения: 10%: 0.3470, 5%: 0.4630, 2.5%: 0.5740, 1%: 0.7390
- **Вывод: Ряд НЕ стационарный**

### 5.3 Определение порядка интегрирования

```python
def check_integration_order(series, max_diff=3):
    diff_series = series.copy()
    order = 0
    
    # Проверка исходного ряда
    if adf_test(diff_series, f'd={order}'):
        return order
    
    # Последовательное дифференцирование
    for d in range(1, max_diff + 1):
        diff_series = diff_series.diff().dropna()
        if adf_test(diff_series, f'd={d}'):
            return d
    
    return max_diff
```

**Результат:** Порядок интегрирования ряда: **d = 1**

---

## 6. Построение модели AR

### 6.1 Частичная автокорреляция (PACF)

```python
# Построение PACF
plot_pacf(series_for_pacf, ax=axes[0], lags=20, method='ywm')
```

### 6.2 Определение порядка модели AR

```python
def determine_ar_order(pacf_values, significance_level=0.05, max_lags=20):
    n = len(pacf_values)
    std_error = 1 / np.sqrt(n)
    threshold = std_error * 1.96
    
    p = 0
    for lag in range(1, max_lags + 1):
        if lag < len(pacf_values):
            if abs(pacf_values[lag]) > threshold:
                p = lag
            else:
                break
    
    return p, threshold, significant_lags
```

**Результаты:**
- Порядок модели AR: **p = 1**
- Порог значимости: 0.4277
- Значимые лаги: [1]

### 6.3 Построение модели AR

```python
# Построение модели AR на исходном ряду
model_ar = AutoReg(df_train['qty'], lags=p)
model_ar_fitted = model_ar.fit()

# Прогноз на один шаг
forecast_ar = model_ar_fitted.forecast(steps=1)
```

**Параметры модели AR:**
| Параметр | Значение | P-значение |
|----------|----------|------------|
| Константа | 89,590 | < 0.001 |
| qty.L1 | 0.6819 | < 0.001 |

**Результаты прогноза:**
| Показатель | Значение |
|------------|----------|
| Прогноз | 205,974.65 |
| Фактическое значение | 423,846.00 |
| Абсолютная ошибка | 217,871.35 |
| Относительная ошибка | 51.40% |

---

## 7. Сравнение результатов

### 7.1 Сравнение всех моделей

| Модель | Прогноз | Факт | Абсолютная ошибка | Относительная ошибка (%) |
|--------|---------|------|-------------------|---------------------------|
| Экспоненциальное сглаживание | 225,015.52 | 423,846.00 | 198,830.48 | 46.91% |
| Модель AR | 205,974.65 | 423,846.00 | 217,871.35 | 51.40% |

### 7.2 Выводы по результатам

1. **Модель экспоненциального сглаживания** показала лучшие результаты:
   - Более низкая абсолютная ошибка (198,830.48 против 217,871.35)
   - Более низкая относительная ошибка (46.91% против 51.40%)

2. **Модель AR** оказалась менее точной:
   - Систематически недооценивает значения
   - Высокая относительная ошибка (51.40%)

3. **Причины низкой точности:**
   - Нестационарность исходного ряда
   - Высокая волатильность данных
   - Прогнозирование на один шаг вперед не учитывает долгосрочные тренды
   - Недостаточный порядок модели AR для учета всей структуры данных

4. **Рекомендации по улучшению:**
   - Использовать дифференцирование для приведения ряда к стационарному виду
   - Рассмотреть модели ARIMA или SARIMA
   - Добавить сезонную компоненту в модель
   - Использовать большее количество лагов в модели AR

---

## Общие выводы

1. Временной ряд товарооборота книжного интернет-магазина не является стационарным, имеет положительный тренд и невыраженную сезонность.

2. Модель экспоненциального сглаживания показала более высокую точность по сравнению с моделью AR первого порядка.

3. Для повышения точности прогнозов рекомендуется использование более сложных моделей, учитывающих структуру ряда.

4. Полученные результаты могут быть использованы для планирования закупок и управления складскими запасами книжного интернет-магазина.
